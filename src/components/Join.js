import React from "react"
import styled from "styled-components"

const Join = () => {
  return <JoinWrapper>hello from join</JoinWrapper>
}

const JoinWrapper = styled.section`
  height: 100vh;
  color: var(--h1);
  position: relative;
  /* width: 90vw;
  margin: 0 auto; */
`
export default Join
